[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# if your main folder has a new name, change it here
packages = ["main/"]

[project]
name = "python-boilerplate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

# If you are using distroless images, it's
# not a good idea to use scripts. Instead,
# your command should be `python -m path.to.file`
[project.scripts]
demo-script = "main.main:demo"

[dependency-groups]
dev = [
    "ipython>=9.17.1",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "ruff>=0.16.9",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = [
    "I", # isort
]

[tool.ruff.lint.isort]
force-single-line = true
